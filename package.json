{
  "name": "priprema-za-intervju",
  "version": "1.0.0",
  "private": true,
  "description": "Priprema za intervju: lokalna stranica za pracenje pripreme za intervju.",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "npm run clean && tsc -p tsconfig.build.json && node scripts/copy-static.mjs",
    "test": "npm run typecheck && node --test tests/*.test.ts",
    "start": "npm run build && node scripts/serve.mjs"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
